interface:
  display_name: "Slonagent"
  short_description: "Repo-specific patterns and workflows for slonagent"
  default_prompt: "Use the slonagent repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true